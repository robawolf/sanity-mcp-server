version: 1
name: sanity-mcp-wrapper
description: Wrapper that runs the official @sanity/mcp-server via npx (stdio)
startCommand:
  type: stdio
  command: npx
  args:
    - -y
    - @sanity/mcp-server@latest


